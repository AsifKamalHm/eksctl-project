apiVersion: v1
kind: Service
metadata:
  name: svc-pyapp
  labels:
    app: pyapp
spec:
  selector:
    app: pyapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer